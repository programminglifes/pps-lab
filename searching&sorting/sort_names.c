#include <stdio.h>
#include <string.h>

int main() {
  int count;
  printf("Enter the number of names you want: ");
  scanf("%d", &count);

  char list[count][30];

  for (int i = 0; i < count; i++) {

  }
}
